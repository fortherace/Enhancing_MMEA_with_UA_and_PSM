function [X, F] = run_hrea_omnitest(N, maxFE)
    clc;

    %% 添加 PlatEMO 路径
    addpath(genpath('D:\Project\PlatEMO\PlatEMO\PlatEMO'));

    N = double(N);
    maxFE = double(maxFE);

    %% 调用 PlatEMO
    [X, F] = platemo( ...
        'algorithm', @HREA, ...
        'problem',   @Omnitest, ...
        'N',         N, ...
        'maxFE',     maxFE, ...
        'draw',      false ...
    );
end
